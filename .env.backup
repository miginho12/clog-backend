# Clog Backend Environment Variables

# 환경 (local | dev | prod)
ENVIRONMENT=local

# 서버
HOST=0.0.0.0
PORT=8000

# 로깅
LOG_LEVEL=DEBUG

# CORS (쉼표 구분 또는 JSON 배열)
CORS_ORIGINS=["http://localhost:3000"]

# Database (다음 세션에 활성화)
# DATABASE_URL=postgresql+asyncpg://clog:clog@localhost:5432/clog
